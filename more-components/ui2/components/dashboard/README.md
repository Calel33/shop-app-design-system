# GPU Cluster Resource Dashboard

Composable dashboard widgets for GPU cluster monitoring: overview stats, stacked usage chart, performance metrics, system status, active jobs, and resource allocation.

Usage:

```tsx
import { GPUDashboard } from '@/ui/components/dashboard/GPUDashboard';
```
